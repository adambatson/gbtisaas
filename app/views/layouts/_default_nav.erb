<ul class="nav navbar-nav">
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
      View a Different Event <span class="caret"></span>
    </a>
    
    <ul class="dropdown-menu">
      <!-- Ongoing events -->
      <% @active_books.each do |book| %>
        <li><a href="#"><%= book.title %> <small class="text-muted">Ongoing</small></a></li>
      <% end %>

      <li role="separator" class="divider"></li>

      <!-- Closed events -->
      <% @archived_books.each do |book| %>
        <li><a href="#"><%= book.title %> <small class="text-muted"><%= time_ago_in_words(book.created_at) %> ago</small></a></li>
      <% end %>
    </ul>
  </li>
</ul>

<ul class="nav navbar-nav navbar-right">
  <li><a href="books.html">Administration</a></li>
</ul>